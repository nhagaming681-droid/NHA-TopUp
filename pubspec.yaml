name: nha_topup
description: NHA TopUp - MLBB Diamond and PUBG UC store.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  firebase_core: ^4.1.0
  cloud_firestore: ^6.0.0
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  image_picker: ^1.1.2
  shared_preferences: ^2.3.2

dev_dependencies:
  firebase_core: ^4.1.0
  cloud_firestore: ^6.0.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
